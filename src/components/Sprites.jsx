import React from 'react'


export const Sprites = ({pokemon}) => {
  const [data, setData] = useState()
  const []
  return (
        <div>
            <img src={`https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/shiny/${pokemon.name}.png`} alt={pokemon.name} />;
        </div>
  )
}
